# In dev, leave API base empty so all fetches go through Vite proxy (`/api → :5000`).
# Same-origin from the browser's view → no CORS preflight, cookies bind to :5173.
VITE_API_BASE_URL=
VITE_SIGNALR_HUB_URL=/auctionHub
